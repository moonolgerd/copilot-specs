{
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "command": "npm run lint --if-present",
        "windows": "npm run lint --if-present"
      }
    ],
    "SessionStart": [
      {
        "type": "command",
        "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"This workspace uses copilot-specs. See .github/instructions/specs/ for feature requirements, design and task files.\"}}' ",
        "windows": "echo {\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"This workspace uses copilot-specs. See .github/instructions/specs/ for feature requirements, design and task files.\"}}"
      }
    ]
  }
}
